// Persian menus + full tutorial text
const { InlineKeyboard } = require('grammy');

function mainMenu() {
  return new InlineKeyboard()
    .text('🔑 ورود به اکانت', 'login').text('📊 وضعیت', 'status').row()
    .text('📡 منابع', 'sources').text('🎯 کانال مقصد', 'dest').row()
    .text('✍️ امضا', 'sign').text('🖼 مدیا', 'media').row()
    .text('⏱ فاصله انتشار', 'interval').text('▶️ شروع / ⏸ توقف', 'toggle').row()
    .text('📖 راهنما', 'help');
}

function backMenu() {
  return new InlineKeyboard().text('🔙 برگشت', 'back');
}

const HELP_TEXT = `📖 راهنمای کامل ربات مدیر اخبار

این ربات با اکانت کاربری تو کانال‌های منبع را می‌خواند، با هوش مصنوعی بازنویسی می‌کند و با ربات توی کانالت منتشر می‌کند.

🔑 ورود به اکانت (فقط یک‌بار):
۱. دکمه ورود به اکانت را بزن
۲. شماره‌ات را با فرمت +98912... بفرست
۳. کدی که به تلگرامت می‌آید را همین‌جا بفرست
۴. اگر رمز دومرحله‌ای داری، آن را هم بفرست
تمام! ورود ذخیره می‌شود و بعد از ری‌استارت سرور هم می‌ماند.

📡 منابع:
آیدی کانال‌ها (مثل @name) یا لینک دعوت (t.me/...) را اضافه یا حذف کن. لیست فعلی را هم می‌توانی ببینی.

🎯 کانال مقصد:
آیدی عددی کانالت را بده (مثل -100...). ربات حتماً باید آنجا ادمین با دسترسی ارسال پیام باشد.

✍️ امضا:
متن پایانی هر خبر، مثلاً @iran_jahan_lahze

🖼 مدیا:
انتخاب کن عکس و فیلم همراه خبر ارسال شود یا فقط متن. فیلم‌ها سنگین‌اند؛ اگر اینترنت سرور کند باشد فقط عکس را روشن بگذار.

⏱ فاصله انتشار:
خبرهای عادی هر چند دقیقه یکی بروند (پیش‌فرض ۲ دقیقه). خبرهای فوری (انفجار، ترور، جنگ) بدون صف و فوری می‌روند.

▶️ شروع / توقف:
رصد را روشن یا متوقف کن.

چطور کار می‌کند؟
۱. هر ۲ دقیقه پیام‌های ۲۰ دقیقه اخیر منابع خوانده می‌شود
۲. تکراری‌ها و تبلیغاتِ واضح بدون مصرف هوش مصنوعی حذف می‌شوند
۳. بقیه به هوش مصنوعی داده می‌شود: اگر انگلیسی یا عربی باشد به فارسی روان ترجمه می‌شود، ویراستاری می‌شود، لینک و امضای منبع پاک می‌شود
۴. خبر ضد نظام یا تبلیغاتی رد می‌شود
۵. خبر عادی می‌رود توی صف، خبر فوری مستقیم منتشر می‌شود
۶. قالب انتشار:
🔴 #خبر_فوری (یا 🔵 #خبر)
متن خبر
امضای تو

دستورات متنی (بدون دکمه هم کار می‌کند):
راهنما | وضعیت | شروع | توقف`;

module.exports = { mainMenu, backMenu, HELP_TEXT };
